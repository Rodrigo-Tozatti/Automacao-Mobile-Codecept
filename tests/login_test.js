/// <reference path='../steps.d.ts' />

Feature('login');

Scenario('Cenário 01 - APP deve realizar login com sucesso', ({ I }) => {
    // Preeencher campo e-mail
    I.fill    // Preencher campo senha
    
    // Clicar botão Entrar

    // Validar login com sucesso
});